node_modules/
dist/
.swa/
# Root index.html must be tracked for Vite build (not deployed; SWA uses dist/)
# Do NOT ignore it here.
staticwebapp.config.json  # Keep only in public/ for Vite to copy to dist/

# === Local env & secrets (never commit) ===
.env
.env.*
!.env.example
.env.local
api/local.settings.json
local.settings.json
swa-token.txt
api/__build_id.txt
public/__build_id.txt

# Test/diagnostic probe files you created
public/ping.txt
public/proof.txt
public/deploy-proof.txt
public/health.txt

# === OS/Editor noise ===
.DS_Store
Thumbs.db

# Guard against accidental nested repo
tabarnam-frontend/

.python_packages
__pycache__
.venv
