# === Dependencies & builds ===
node_modules/
dist/
.swa/
index.html  # Development entry point - keep locally, don't deploy
staticwebapp.config.json  # Keep only in public/ for Vite to copy to dist/

# === Local env & secrets (never commit) ===
.env
.env.*
!.env.example
.env.local
api/local.settings.json
local.settings.json
swa-token.txt

# Test/diagnostic probe files you created
public/ping.txt
public/proof.txt
public/deploy-proof.txt
public/health.txt

# === OS/Editor noise ===
.DS_Store
Thumbs.db

# Guard against accidental nested repo
tabarnam-frontend/

.python_packages
__pycache__
.venv

